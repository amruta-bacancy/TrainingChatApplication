<%= form_with(model: @room, remote: true, class: "d-flex" ) do |f| %>
  <%= f.text_field :name, class: "form-control", autocomplete: 'off' %><br>
  <%= f.submit data: { disable_with: false } %><br>
<% end %>
