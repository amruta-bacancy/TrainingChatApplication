<div class="cont-<%= message.user.id %>">
  <div class="message-box msg-<%= message.user.id %> " >
    <% unless message.room.is_private %>
      <h6 class="name"> <%= message.user.username %> </h6>
    <% end %>
    
       <%# @message.header_image.each do |file| %>
   <%#= link_to file.filename, rails_blob_path(file, disposition: :attachment) %>
 <%# end %>

 <%# if @messages.header_images.present? %>
 <%# message.attacments %>
 <%# end %>

  <%= message.content %>
  <%# binding.pry %>
  <%= image_tag((message.header_image), size: "150") if message.header_image.attached? %>

<%# if message.header_image %>
    <%#= image_tag message.header_image %>
 <%# end %>


  </div>
</div>